# Orchestration Log: Gately (2026-03-04T21:26Z)

**Session ID:** 2026-03-04T2126-gately  
**Agent:** Gately (Game Dev)  
**Mode:** Background  
**Duration:** [execution time]  
**Status:** ✅ Complete

## Spawn Context

User directive: **Territory Control Core Identity Pivot** — Territory control with immutable HQ + conquerable expansion. Gately tasked with rendering analysis and visual design proposal.

## Work Delivered

### Files Produced
- ✅ `.squad/decisions/inbox/gately-territory-rendering.md` — Rendering layer analysis + implementation checklist
- ✅ `.squad/agents/gately/history.md` — Updated with territory rendering proposal

### Key Deliverables
- **Territory Rendering Analysis** (272 lines)
  - Current state assessment: GridRenderer solid foundation, pre-allocated Graphics overlays, Colyseus state binding
  - Identified current capabilities: color-coded territory borders, claiming animation (pulsing), territory vs. open distinction, building integrity tracking
  - Identified gaps: no visual distinction between immutable vs. conquered, no contested territory visual, no influence visualization, no damage state indication

### Proposed Rendering Layers

1. **Immutable vs. Conquered Territory** (~15 lines)
   - Visual distinction via overlay color/opacity
   - Immutable: solid fill (low alpha 0.15)
   - Conquered: dashed border (higher alpha 0.6)
   - Uses existing stroke/fill API; no new render objects

2. **Contested Territory** (~30 lines)
   - Detect multiple influences per tile via `influencingPlayerIds[]`
   - Render stripe/hatch pattern when contested
   - Approach 1: Dashed pattern (recommended for MVP)
   - Approach 2: Color blend (elegant but complex)
   - Approach 3: Multi-player border ring (highly visible)
   - Cost: negligible perf, uses existing overlay graphics

3. **Influence Visualization** (~15 lines Phase 2)
   - Optional polish: gradient/heat map showing control strength
   - Phase 1 MVP: skip (shows contested tiles with higher border alpha)
   - Phase 2: Add `influenceStrength: number` (0–1) → render as overlay alpha intensity

4. **Territory Health/Integrity** (~10 lines)
   - Render health state via border thickness + saturation
   - 100% health: solid thick border, bright alpha
   - 50% health: mixed dashed/solid, desaturated
   - 1% health: dashed thin, faded alpha
   - Uses existing `shapeHP: number` tracking

5. **Multi-Player Territory Overview** (deferred Phase 2+)
   - Optional minimap (skip for MVP, use legend in HUD instead)
   - Alternative: Expand HudDOM territory score display, add player color legend

### Implementation Checklist

**Must Have (MVP):**
- [ ] Add `isImmutable` metadata to tile state (or compute from HQ distance)
- [ ] Update `GridRenderer.updateTerritoryOverlay()` for immutable territory (solid low-alpha fill)
- [ ] Verify border rendering works for conquered territory
- [ ] Test with existing shape block system

**Should Have (Phase 2):**
- [ ] Add `influencingPlayerIds[]` to tile state for contest detection
- [ ] Update overlay stripe/hatch when contested
- [ ] Add `influenceStrength: number` for future gradient visualization

**Nice to Have (Phase 3+):**
- [ ] Minimap in corner (or skip, use HUD legend)
- [ ] Damage animation/pulsing for low-health territories
- [ ] Gradient border intensity based on active threats

### Code Locations (No changes needed for MVP)
- `GridRenderer.ts` lines 127–193: `updateTerritoryOverlay()` — where territory visual drawn
- `HudDOM.ts` lines 166–168: Territory score display (can expand to show all players)
- `index.html`: Shape carousel — add legend for player colors if minimap deferred

### Performance Notes
- No new render objects: uses existing overlay Graphics per tile
- No new data structures: tile state already extended (shapeHP, ownerID, claimingPlayerID)
- O(N) update cost per state change, O(1) per tile per frame
- Pre-allocated overlays in buildGrid()
- Pulsing claiming animation already runs per frame; can extend for other animations

### Outcome
- Confirmed GridRenderer **production-ready for territory distinction**
- Identified 4 rendering layers with minimal scope impact (~50 lines total for MVP)
- Provided implementation checklist (MVP, Phase 2, Phase 3+)
- Confirmed existing renderer can handle all proposed changes
- No breaking changes to current rendering loop

## Cross-Agent Dependencies

- **Hal & Pemulis:** Define data fields (isImmutable, influencingPlayerIds[], influenceStrength)
- **Pemulis:** Server sends `isImmutable` flag OR HQ distance for client computation
- **Pemulis:** Server sends `influencingPlayerIds[]` per tile for contest visualization
- **User (dkirby-ms):** Confirm immutable territory bounds (always 9×9 centered on HQ, or variable?)

## Architecture Decisions

- **Overlay-based rendering:** Reuses existing Graphics per tile; no dynamic allocation
- **Immutable vs. conquered visual distinction:** Solid fill vs. dashed border (intuitive, low cost)
- **Contest visualization:** Stripe pattern MVP (simplest), color blend Phase 2 if needed
- **Health visualization:** Border thickness + alpha (composable with ownership color)

## Risk Assessment

- **Visual clarity:** Players must understand influence at glance (color gradients are critical)
- **Contested priority:** If 3+ players influence a tile, which colors to show? (Recommend top 2 by strength)
- **Performance:** Overlay rendering currently O(N); no chunking yet, but scale is 64×64 = 4k tiles (acceptable)

## Metrics

- **Rendering scope:** ~50 lines total MVP (immutable + conquered distinction only)
- **No perf regression:** Uses existing overlay system, no new allocations
- **Readiness:** MVP can start immediately once Pemulis provides data fields; Phase 2 deferred pending playtesting

---

**Status:** ✅ COMPLETE — Rendering layers proposed, implementation checklist ready, awaiting server data field confirmation.
